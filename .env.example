# xAI / Grok — required for the Counsel agent
XAI_API_KEY=
# Optional model override (default: grok-3)
# XAI_MODEL=grok-3

# Staff quote inbox (/admin/quotes)
ADMIN_STAFF_TOKEN=

# Optional ops email when a quote is created
ORDER_NOTIFY_EMAIL=
RESEND_API_KEY=
RESEND_FROM=
RESEND_TO=

# Optional Supabase auth (magic link) — when set, /auth uses OTP
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
